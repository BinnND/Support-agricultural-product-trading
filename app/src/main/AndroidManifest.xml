<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Quyền cần thiết -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" /> <!-- Khai báo tính năng camera (fix lỗi warning) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <queries>
        <package android:name="com.google.android.gms" />
    </queries>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Htgdnss"
        tools:targetApi="31">
        <activity
            android:name=".common.ProductReviewsActivity"
            android:exported="false" />
        <activity
            android:name=".seller.ReplyToReviewActivity"
            android:exported="false" />
        <activity
            android:name=".admin.ManageUsersActivity"
            android:exported="false" />
        <activity
            android:name=".buyer.AddReviewActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Auth -->
        <activity android:name=".auth.LoginActivity" />
        <activity android:name=".auth.RegisterActivity" />
        <activity android:name=".auth.ForgotPasswordActivity" /> <!-- Common -->
        <activity android:name=".common.ProfileActivity" /> <!-- Buyer -->
        <activity android:name=".buyer.HomeBuyerActivity" />
        <activity android:name=".buyer.SearchActivity" />
        <activity android:name=".buyer.ProductDetailActivity" />
        <activity android:name=".buyer.CartActivity" />
        <activity android:name=".buyer.MyOrdersBuyerActivity" />
        <activity android:name=".buyer.OrderDetailActivity" /> <!-- Seller -->
        <activity android:name=".seller.AddProductActivity" />
        <activity android:name=".seller.MyProductsActivity" />
        <activity android:name=".seller.ManageOrdersSellerActivity" />
        <activity android:name=".seller.XacNhanDangActivity" />
        <activity android:name=".seller.XacNhanViTriActivity" />
        <activity android:name=".seller.ChupAnhActivity" /> <!-- Admin -->
        <activity android:name=".admin.AdminDashboardActivity" /> <!-- FCM Service -->
        <service
            android:name=".service.FcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.htgdnss.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>
    </application>

</manifest>