<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Quyền cần thiết -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- Khai báo tính năng camera (fix lỗi warning) -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Htgdnss"
        tools:targetApi="31">

            <activity
                android:name=".MainActivity"
                android:exported="true">
                <intent-filter>
                    <action android:name="android.intent.action.MAIN" />
                    <category android:name="android.intent.category.LAUNCHER" />
                </intent-filter>
            </activity>

        <!-- Auth -->
        <activity android:name=".auth.LoginActivity" />
        <activity android:name=".auth.RegisterActivity" />
        <activity android:name=".auth.ForgotPasswordActivity" />

        <!-- Common -->
        <activity android:name=".common.ProfileActivity" />

        <!-- Buyer -->
        <activity android:name=".buyer.HomeBuyerActivity" />
        <activity android:name=".buyer.SearchActivity" />
        <activity android:name=".buyer.ProductDetailActivity" />
        <activity android:name=".buyer.CartActivity" />
        <activity android:name=".buyer.MyOrdersBuyerActivity" />

        <!-- Seller -->
        <activity android:name=".seller.AddProductActivity" />
        <activity android:name=".seller.MyProductsActivity" />
        <activity android:name=".seller.ManageOrdersSellerActivity" />

        <!-- Admin -->
        <activity android:name=".admin.AdminDashboardActivity" />

        <!-- FCM Service -->
        <service
            android:name=".service.FcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>
</manifest>